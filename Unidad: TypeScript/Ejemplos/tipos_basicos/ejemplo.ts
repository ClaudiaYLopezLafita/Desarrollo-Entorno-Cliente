function rollDice(userName:string, maxTries:number){
    const result:string[] =[];
    const MAX_DICE = 6;

    for(let i=0;i<maxTries;i++){
        const result = Math.ceil(Math.random()*MAX_DICE);
    }
}